# Development environment
# The sync server is now in a separate repo: https://github.com/evcraddock/todu-sync
# Install it with: cargo install --git https://github.com/evcraddock/todu-sync
server: RUST_LOG=todu_sync=debug todu-sync
