# Build artifacts
/target

# Development environment
.env
.overmind.sock

# Data directories
/data/

# Server config (may contain secrets)
/config/server.yaml

# Web build artifacts (also in web/.gitignore)
/web/node_modules/
/web/dist/
/web/dist-server/
/web/*.sqlite*
